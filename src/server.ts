import app from './app';
// import {} from "./data-source"
const port = process.env.PORT || 3000;

// (async () => {
//   await _.initialize().catch((err) =>
//     console.log('Error during data source initialization', err)
//   );

//   app.listen(port, () => {
//     console.log('Server running', port);
//   });
// })();
